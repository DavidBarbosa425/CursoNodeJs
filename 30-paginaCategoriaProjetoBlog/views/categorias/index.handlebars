<h1>Lista de categorias: </h1>
{{#each categorias}}

    <div class="card">
        <div class="card-body">
              <a href="/categorias/{{slug}}"><h5>{{nome}}</h5></a>  
                <p>{{slug}}</p>
        </div>
    </div>

{{else}}

{{/each}}