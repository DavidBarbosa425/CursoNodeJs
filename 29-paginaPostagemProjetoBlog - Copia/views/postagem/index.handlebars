<div class="card">
    <div class="card-body">
        {{#each postagem}}
            <h1>{{titulo}} </h1>
                <p>{{data}} </p>
            <p>{{conteudo}}</p>
        {{else}}

        {{/each}}
    </div>
</div>

