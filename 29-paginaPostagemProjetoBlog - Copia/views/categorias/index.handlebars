<h1>Lista de categorias: </h1>
{{#each categorias}}

    <div class="card">
        <div class="card-body">
                <h5>{{nome}}</h5>
                <p>{{slug}}</p>
        </div>
    </div>

{{else}}

{{/each}}